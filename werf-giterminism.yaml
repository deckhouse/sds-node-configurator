giterminismConfigVersion: 1
config:
  goTemplateRendering:	# The rules for the Go-template functions
    allowEnvVariables: [ /CI_.+/, SOURCE_REPO, MODULE_EDITION ]
  stapel:
    mount:
     allowFromPaths:
     - ~/go-pkg-cache
