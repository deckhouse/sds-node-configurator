# Расширение базового конфига — исключение для xxhash.h (false positive)
[extend]
useDefault = false
path = "/tmp/gitleaks.base.toml"

[allowlist]
paths = [
  '''(?:^|/)images/.*\.patch$''',
  '''src/sds-node-configurator__agent/util-linux/include/xxhash\.h$''',
]
